# Base URL for Playwright (hostname must resolve, e.g. /etc/hosts: <ip> lede)
PLAYWRIGHT_BASE_URL=http://lede

# LuCI session login (same as SSH root)
LUCI_USER=root
LUCI_PASSWORD=

# Optional: run deploy.sh before tests (requires SSH access to router)
# E2E_DEPLOY=1
# DEPLOY_ROUTER=192.168.100.2
